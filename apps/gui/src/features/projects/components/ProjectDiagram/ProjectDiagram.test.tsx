import { describe, expect, test } from "vitest";
import ProjectDiagram from "./ProjectDiagram";
import { render } from "~test-utils";

describe("Project Diagram", () => {
    test.todo("renders", () => {
        render(<ProjectDiagram />);
        expect(true).toBe(true);
    });
});
